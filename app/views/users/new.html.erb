<!-- <h1>Sign Up</h1>
    <div class="row">
      <div class="col-lg-6 col-lg-offset-3">
        <div class="jumbotron">
        <%= form_for @user do |f| %>
          <% if @user.errors.any? %>
            <div class="error_messages">
              <h2>Form is invalid</h2>
              <ul>
                <% @user.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="field">
            <%= f.label :first_name %>
            <%= f.text_field :first_name %>
          </div><br />

          <div class="field">
            <%= f.label :last_name %>
            <%= f.text_field :last_name %>
          </div><br />

          <div class="field">
            <%= f.label :email %>
            <%= f.email_field :email %>
          </div><br />
          <div class="field">
            <%= f.label :password %>
            <%= f.password_field :password %>
          </div><br />
          <div class="field">
            <%= f.label :password_confirmation %>
            <%= f.password_field :password_confirmation %>
          </div><br />
          <div class="action"><%= f.submit %></div>
        <% end %>
    </div>
  </div>

</div> -->

<div class="form">

      <ul class="tab-group">
        <li class="tab <% unless @isLogin %> active <% end %>"><a href="/signup">Sign Up</a></li>
        <li class="tab <% if @isLogin %> active <% end %>"><a href="/login">Log In</a></li>
      </ul>

      <div class="tab-content">
        <div id="signup" class="<% unless @isLogin %> show <% else %> hide <% end %> ">
          <h2>Sign Up for Free</h2>

        <%= form_for @user do |f| %>
          <form action="/signup" method="post">

          <div class="top-row">
            <div class="field-wrap">
              <!-- <label>
                First Name<span class="req">*</span>
              </label> -->
              <%= f.text_field :first_name, placeholder: 'First Name', class: 'req' %>
            </div>

            <div class="field-wrap">
              <%= f.text_field :last_name, placeholder: 'Last Name' %>
            </div>
          </div>

          <div class="field-wrap">
            <%= f.email_field :email, placeholder: 'Email' %>
          </div>

          <div class="field-wrap">
              <%= f.password_field :password, placeholder: 'Password' %>
          </div>

          <div class="field-wrap">
              <%= f.password_field :password_confirmation, placeholder: 'Password Confirmation' %>
          </div>

          <div class="action"><%= f.submit :'Get Started', class: "button button-block" %></div>

          </form>
        <% end %>
        </div>



        <div id="login" class="<% if @isLogin %> show <% else %> hide <% end %> ">
          <h1>Welcome Back!</h1>

          <%= form_tag sessions_path do %>

          <div class="field-wrap">
            <%= text_field_tag :email, params[:email] %>
          </div>

          <div class="field-wrap">
            <%= password_field_tag :password, params[:email] %>
          </div>

          <!-- <p class="forgot"><a href="#">Forgot Password?</a></p> -->

          <!-- <button class="button button-block"/>Log In</button> -->
          <div class="action"><%= submit_tag "Log In", class: "button button-block" %></div>

           <% end %>
        </div>

      </div><!-- tab-content -->

</div> <!-- /form -->
